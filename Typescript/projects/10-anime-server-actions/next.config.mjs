/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      domains: ['m.media-amazon.com', 'static.wikia.nocookie.net', 'flxt.tmsimg.com', 'i.pinimg.com', 'shikimori.one'],
    },
};

export default nextConfig;
